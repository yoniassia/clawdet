# Clawdet Architecture - Visual Diagrams

## System Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                      INTERNET                                     │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                   ┌─────────┴─────────┐
                   │   Cloudflare CDN   │
                   │  • SSL/TLS         │
                   │  • DDoS Protection │
                   │  • DNS             │
                   └─────────┬─────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐  ┌──────────────────┐  ┌───────────────┐
│ clawdet.com   │  │ user1.clawdet    │  │ user2.clawdet │
│ (Main App)    │  │ (Instance 1)     │  │ (Instance 2)  │
└───────────────┘  └──────────────────┘  └───────────────┘
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐  ┌──────────────────┐  ┌───────────────┐
│ Next.js App   │  │ Docker Container │  │ Docker Container│
│ PM2           │  │ OpenClaw + Grok  │  │ OpenClaw + Grok │
│ Port 3002     │  │ Port 80 → 8080   │  │ Port 80 → 8080  │
└───────┬───────┘  └─────────┬────────┘  └───────┬───────┘
        │                    │                    │
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐  ┌──────────────────┐  ┌───────────────┐
│ JSON Database │  │ User Workspace   │  │ User Workspace│
│ users.json    │  │ ~/.openclaw/     │  │ ~/.openclaw/  │
└───────────────┘  └──────────────────┘  └───────────────┘
        │
        │
        ▼
┌───────────────────────────────────┐
│     External Services             │
│                                   │
│  • Hetzner Cloud (VPS)            │
│  • Cloudflare (DNS + SSL)         │
│  • X.AI (Grok 4.2 API)            │
│  • Anthropic (Claude fallback)    │
│  • Twitter OAuth                  │
└───────────────────────────────────┘
```

---

## User Signup Flow (Email)

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Visit clawdet.com
     ▼
┌─────────────────┐
│   Homepage      │
│  (app/page.tsx) │
└────┬────────────┘
     │
     │ 2. Fill signup form
     │    (name, email, password)
     ▼
┌────────────────────────┐
│ POST /api/auth/register│
│                        │
│ • Validate inputs      │
│ • Hash password (bcrypt)│
│ • Check if email exists│
└────┬───────────────────┘
     │
     │ 3. Create user
     ▼
┌─────────────────┐
│  users.json     │
│  {              │
│    id: "user_"  │
│    email: "..." │
│    passwordHash │
│    paid: true   │
│  }              │
└────┬────────────┘
     │
     │ 4. Auto-login (NextAuth)
     ▼
┌──────────────────┐
│ Set session cookie│
│ (HttpOnly, Secure)│
└────┬─────────────┘
     │
     │ 5. Redirect
     ▼
┌─────────────────────┐
│ /signup/details     │
│                     │
│ • Enter email again │
│ • Accept terms      │
└────┬────────────────┘
     │
     │ 6. Submit
     ▼
┌──────────────────────┐
│ POST /signup/complete│
│                      │
│ • Save email + terms │
│ • Confirm paid: true │
└────┬─────────────────┘
     │
     │ 7. Redirect
     ▼
┌──────────────────┐
│   /dashboard     │
│                  │
│ • Free beta!     │
│ • Provision CTA  │
└──────────────────┘
```

---

## VPS Provisioning Flow

```
┌────────────┐
│ Dashboard  │
│ Click: Get │
│ Free Inst. │
└─────┬──────┘
      │
      │ POST /api/provisioning/start
      ▼
┌───────────────────────┐
│ Provisioner Service   │
│ (lib/provisioner-v2)  │
└───────┬───────────────┘
        │
        │ Step 1: Create VPS
        ▼
┌───────────────────────┐
│  Hetzner Cloud API    │
│                       │
│  POST /servers        │
│  {                    │
│    name: "clawdet-u1" │
│    type: "cx23"       │
│    image: "ubuntu"    │
│    location: "hel1"   │
│  }                    │
└───────┬───────────────┘
        │
        │ ⏱️  ~60 seconds
        ▼
┌───────────────────────┐
│  VPS Created          │
│  IP: 65.109.x.x       │
│  Status: running      │
└───────┬───────────────┘
        │
        │ Step 2: Configure DNS
        ▼
┌───────────────────────┐
│  Cloudflare API       │
│                       │
│  POST /dns_records    │
│  {                    │
│    type: "A"          │
│    name: "user1"      │
│    content: "65.109.."│
│    proxied: true      │
│  }                    │
└───────┬───────────────┘
        │
        │ ⏱️  ~10 seconds
        ▼
┌───────────────────────┐
│  DNS Configured       │
│  user1.clawdet.com    │
│  → 65.109.x.x         │
│  SSL: Auto (CF)       │
└───────┬───────────────┘
        │
        │ Step 3: SSH Install
        ▼
┌───────────────────────┐
│  SSH Connection       │
│  root@65.109.x.x      │
│                       │
│  ssh-installer-v2.ts  │
└───────┬───────────────┘
        │
        │ Install Docker
        ▼
┌───────────────────────┐
│  curl -fsSL           │
│  get.docker.com | sh  │
└───────┬───────────────┘
        │
        │ Download files
        ▼
┌───────────────────────┐
│  wget clawdet.com/    │
│  • provision.sh       │
│  • docker-compose.yml │
└───────┬───────────────┘
        │
        │ Create .env
        ▼
┌───────────────────────┐
│  .env                 │
│  XAI_API_KEY=xai-..   │
│  GATEWAY_TOKEN=...    │
│  MODEL=grok-420-...   │
└───────┬───────────────┘
        │
        │ Docker Compose Up
        ▼
┌───────────────────────┐
│  docker compose up -d │
│                       │
│  Pull: coollabsio/    │
│        openclaw:latest│
│  Start: Port 80:8080  │
└───────┬───────────────┘
        │
        │ ⏱️  ~5 minutes
        ▼
┌───────────────────────┐
│  🎉 Instance Ready!   │
│                       │
│  user1.clawdet.com    │
│  Grok 4.2 reasoning   │
│  Gateway: :18789      │
└───────────────────────┘
```

---

## Authentication Flow (X OAuth)

```
┌──────┐
│ User │
└──┬───┘
   │
   │ 1. Click "Continue with X"
   ▼
┌──────────────────┐
│ GET /api/auth/   │
│     x/login      │
│                  │
│ • Generate PKCE  │
│   - verifier     │
│   - challenge    │
│ • Set cookies    │
└──┬───────────────┘
   │
   │ 2. Redirect to Twitter
   ▼
┌──────────────────────────┐
│  twitter.com/i/oauth2/   │
│       authorize          │
│                          │
│  ?client_id=UUx2N...     │
│  &redirect_uri=          │
│   clawdet.com/callback   │
│  &code_challenge=...     │
│  &code_challenge_method  │
│   =S256                  │
└──┬───────────────────────┘
   │
   │ 3. User authorizes
   ▼
┌──────────────────────────┐
│  Twitter Auth Screen     │
│                          │
│  Clawdet wants to:       │
│  ✓ Read your profile     │
│  ✓ Read tweets           │
│                          │
│  [ Authorize App ]       │
└──┬───────────────────────┘
   │
   │ 4. Callback with code
   ▼
┌──────────────────────────┐
│ GET /api/auth/x/callback │
│   ?code=Ag8K...          │
│   &state=abc123          │
│                          │
│ • Verify state cookie    │
│ • Verify PKCE            │
└──┬───────────────────────┘
   │
   │ 5. Exchange code
   ▼
┌──────────────────────────┐
│ POST twitter.com/oauth2/ │
│      token               │
│                          │
│ Authorization:           │
│   Basic <client:secret>  │
│ Body:                    │
│   code=Ag8K...           │
│   code_verifier=...      │
└──┬───────────────────────┘
   │
   │ 6. Receive tokens
   ▼
┌──────────────────────────┐
│ {                        │
│   access_token: "abc.."  │
│   token_type: "bearer"   │
│   expires_in: 7200       │
│   refresh_token: "xyz.." │
│ }                        │
└──┬───────────────────────┘
   │
   │ 7. Fetch profile
   ▼
┌──────────────────────────┐
│ GET api.twitter.com/2/   │
│     users/me             │
│                          │
│ Authorization: Bearer    │
│   abc...                 │
└──┬───────────────────────┘
   │
   │ 8. User data
   ▼
┌──────────────────────────┐
│ {                        │
│   data: {                │
│     id: "206662545"      │
│     username: "yoniassia"│
│     name: "Yoni Assia"   │
│   }                      │
│ }                        │
└──┬───────────────────────┘
   │
   │ 9. Create/update user
   ▼
┌──────────────────────────┐
│  users.json              │
│  {                       │
│    xId: "206662545"      │
│    xUsername: "yoniassia"│
│    paid: true            │
│  }                       │
└──┬───────────────────────┘
   │
   │ 10. Set session
   ▼
┌──────────────────────────┐
│  Cookie: user_session    │
│  Value: <token>          │
│  HttpOnly: true          │
│  Secure: true            │
└──┬───────────────────────┘
   │
   │ 11. Redirect
   ▼
┌──────────────────────────┐
│  /signup/details         │
└──────────────────────────┘
```

---

## Component Relationships

```
┌─────────────────────────────────────────────┐
│              FRONTEND LAYER                  │
├─────────────────────────────────────────────┤
│                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ Homepage │  │  Signup  │  │Dashboard │  │
│  │          │  │          │  │          │  │
│  │ • Hero   │  │ • X Auth │  │ • Status │  │
│  │ • Trial  │  │ • Email  │  │ • Provision │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  │
│       │             │             │         │
└───────┼─────────────┼─────────────┼─────────┘
        │             │             │
┌───────┼─────────────┼─────────────┼─────────┐
│       │      API LAYER            │         │
├───────┼─────────────┼─────────────┼─────────┤
│       ▼             ▼             ▼         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ Trial API│  │ Auth API │  │Provision │  │
│  │          │  │          │  │   API    │  │
│  │ /trial   │  │ /auth/*  │  │/provision│  │
│  │ -chat    │  │ /register│  │ -ing/*   │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  │
│       │             │             │         │
└───────┼─────────────┼─────────────┼─────────┘
        │             │             │
┌───────┼─────────────┼─────────────┼─────────┐
│       │      SERVICE LAYER        │         │
├───────┼─────────────┼─────────────┼─────────┤
│       ▼             ▼             ▼         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ X.AI API │  │  Database│  │ Provision│  │
│  │          │  │          │  │  Service │  │
│  │ • Grok   │  │ • CRUD   │  │ • VPS    │  │
│  │ • Stream │  │ • Session│  │ • DNS    │  │
│  └──────────┘  └────┬─────┘  └────┬─────┘  │
│                     │             │         │
└─────────────────────┼─────────────┼─────────┘
                      │             │
┌─────────────────────┼─────────────┼─────────┐
│            DATA / INFRASTRUCTURE            │
├─────────────────────┼─────────────┼─────────┤
│                     ▼             ▼         │
│  ┌──────────────────────┐  ┌──────────────┐│
│  │   users.json         │  │  Hetzner     ││
│  │   • User records     │  │  • VPS       ││
│  │   • Sessions         │  │  • Networks  ││
│  │   • Instances        │  │              ││
│  └──────────────────────┘  └──────┬───────┘│
│                                   │        │
│  ┌──────────────────────┐         ▼        │
│  │   Cloudflare         │  ┌──────────────┐│
│  │   • DNS records      │  │  Docker      ││
│  │   • SSL certs        │  │  Containers  ││
│  │   • Proxy            │  │  • OpenClaw  ││
│  └──────────────────────┘  └──────────────┘│
└─────────────────────────────────────────────┘
```

---

## Database Schema (Current: JSON)

```
┌──────────────────────────────────────┐
│           users.json                 │
├──────────────────────────────────────┤
│                                      │
│  ┌────────────────────────────────┐ │
│  │  User                           │ │
│  ├────────────────────────────────┤ │
│  │  id: string (PK)               │ │
│  │  email: string (UNIQUE)        │ │
│  │  passwordHash?: string         │ │
│  │  name?: string                 │ │
│  │  ─────────────────────────────  │ │
│  │  xId?: string (UNIQUE)         │ │
│  │  xUsername?: string            │ │
│  │  xName?: string                │ │
│  │  xProfileImage?: string        │ │
│  │  ─────────────────────────────  │ │
│  │  paid: boolean                 │ │
│  │  paymentMethod?: string        │ │
│  │  paidAt?: string               │ │
│  │  ─────────────────────────────  │ │
│  │  provisioningStatus?: string   │ │
│  │  instanceUrl?: string          │ │
│  │  hetznerVpsId?: string         │ │
│  │  hetznerVpsIp?: string         │ │
│  │  ─────────────────────────────  │ │
│  │  sessionToken?: string (UNIQUE)│ │
│  │  sessionCreatedAt?: number     │ │
│  │  termsAccepted?: boolean       │ │
│  │  emailVerified?: boolean       │ │
│  │  ─────────────────────────────  │ │
│  │  createdAt: number             │ │
│  │  updatedAt: number             │ │
│  └────────────────────────────────┘ │
│                                      │
│  Access Patterns:                    │
│  • findUserById(id)                  │
│  • findUserByEmail(email)            │
│  • findUserByXId(xId)                │
│  • findUserBySessionToken(token)     │
│  • updateUser(id, updates)           │
│                                      │
└──────────────────────────────────────┘
```

---

## Deployment Topology

```
┌─────────────────────────────────────────────────────────────┐
│                      PRODUCTION                              │
└─────────────────────────────────────────────────────────────┘

┌───────────────────────┐         ┌───────────────────────┐
│   Primary Server      │         │  Cloudflare Global    │
│   188.34.197.212      │◄────────┤  Edge Network         │
│                       │         │                       │
│  ┌─────────────────┐  │         │  • DNS                │
│  │ Caddy Proxy     │  │         │  • SSL/TLS            │
│  │ :80/:443        │  │         │  • DDoS Protection    │
│  └────────┬────────┘  │         │  • CDN Cache          │
│           │           │         └───────────────────────┘
│  ┌────────▼────────┐  │
│  │ PM2             │  │
│  │ clawdet-prod    │  │         ┌───────────────────────┐
│  │ :3002           │  │         │  Hetzner Cloud        │
│  └────────┬────────┘  │         │  Europe (hel1)        │
│           │           │         │                       │
│  ┌────────▼────────┐  │         │  Customer VPS Pool:   │
│  │ Next.js App     │  │         │  ┌─────────────────┐  │
│  │ • Frontend      │  │         │  │ user1.clawdet   │  │
│  │ • API Routes    │  │◄────────┼─▶│ cx23: 4GB       │  │
│  │ • Auth          │  │         │  │ Docker          │  │
│  └────────┬────────┘  │         │  └─────────────────┘  │
│           │           │         │  ┌─────────────────┐  │
│  ┌────────▼────────┐  │         │  │ user2.clawdet   │  │
│  │ users.json      │  │         │  │ cx23: 4GB       │  │
│  │ (Temporary DB)  │  │         │  │ Docker          │  │
│  └─────────────────┘  │         │  └─────────────────┘  │
└───────────────────────┘         │  ... (more instances) │
                                  └───────────────────────┘

┌───────────────────────────────────────────────────────────┐
│                   EXTERNAL SERVICES                        │
└───────────────────────────────────────────────────────────┘

┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  X.AI API    │    │ Anthropic    │    │  Twitter     │
│  Grok 4.2    │    │ Claude 4.5   │    │  OAuth 2.0   │
│              │    │ (Fallback)   │    │              │
└──────────────┘    └──────────────┘    └──────────────┘
```

---

## Data Flow: Provisioning

```
User Dashboard
      │
      │ 1. Click "Get My Free Instance"
      ▼
┌─────────────────┐
│ POST /api/      │
│ provisioning/   │
│ start           │
└─────┬───────────┘
      │
      │ 2. Verify auth
      ▼
┌─────────────────┐
│ Check session   │
│ Check paid:true │
└─────┬───────────┘
      │
      │ 3. Queue job
      ▼
┌─────────────────┐
│ provisionUser   │
│ Instance(userId)│
└─────┬───────────┘
      │
      ├──────────────────────────────────┐
      │                                  │
      │ 4a. Create VPS                   │ 4b. Update status
      ▼                                  ▼
┌──────────────┐                  ┌──────────────┐
│ Hetzner API  │                  │  users.json  │
│ POST /servers│                  │  status:     │
│              │                  │  creating_vps│
│ Returns:     │                  └──────────────┘
│ • VPS ID     │
│ • IP address │
└─────┬────────┘
      │
      │ 5. Configure DNS
      ▼
┌──────────────────┐
│ Cloudflare API   │
│ POST /dns_records│
│                  │
│ A record:        │
│ user1 → 65.109.x │
└─────┬────────────┘
      │
      │ 6. Wait for DNS
      ▼
┌──────────────────┐
│ Poll DNS         │
│ Until resolved   │
└─────┬────────────┘
      │
      │ 7. SSH connect
      ▼
┌──────────────────┐
│ SSH to VPS       │
│ root@65.109.x.x  │
└─────┬────────────┘
      │
      │ 8. Install Docker
      ▼
┌──────────────────┐
│ curl get.docker  │
│ systemctl start  │
└─────┬────────────┘
      │
      │ 9. Deploy OpenClaw
      ▼
┌──────────────────┐
│ docker compose   │
│ up -d            │
│                  │
│ Image: coollabsio│
│ /openclaw:latest │
└─────┬────────────┘
      │
      │ 10. Health check
      ▼
┌──────────────────┐
│ GET /healthz     │
│ 200 OK?          │
└─────┬────────────┘
      │
      │ 11. Update DB
      ▼
┌──────────────────┐
│ users.json       │
│ status: complete │
│ instanceUrl:     │
│ user1.clawdet.com│
└─────┬────────────┘
      │
      │ 12. Notify user
      ▼
┌──────────────────┐
│ Dashboard shows: │
│ 🎉 Ready!        │
│ Link to instance │
└──────────────────┘
```

---

## Security Layers

```
┌─────────────────────────────────────────┐
│         PERIMETER SECURITY              │
├─────────────────────────────────────────┤
│  Cloudflare:                            │
│  ✓ DDoS Protection (automatic)          │
│  ✓ SSL/TLS (TLS 1.3)                    │
│  ✓ WAF (Web Application Firewall)       │
│  ✓ Rate limiting (L7)                   │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│         APPLICATION SECURITY            │
├─────────────────────────────────────────┤
│  Next.js Middleware:                    │
│  ✓ Security headers                     │
│    - X-Frame-Options: DENY              │
│    - X-Content-Type-Options: nosniff    │
│    - CSP (Content Security Policy)      │
│    - Referrer-Policy                    │
│                                         │
│  API Routes:                            │
│  ✓ Rate limiting (IP-based)             │
│    - Auth: 5 req/min                    │
│    - Register: 5 req/hour               │
│  ✓ Input validation                     │
│  ✓ Input sanitization                   │
│  ✓ CORS policy                          │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│         AUTHENTICATION                  │
├─────────────────────────────────────────┤
│  Password Auth:                         │
│  ✓ bcrypt hashing (12 rounds)           │
│  ✓ Min length: 8 characters             │
│  ✓ No password in logs/errors           │
│                                         │
│  OAuth (X/Twitter):                     │
│  ✓ SHA256 PKCE                          │
│  ✓ State verification (CSRF)            │
│  ✓ Secure cookie storage                │
│  ✓ Token expiration                     │
│                                         │
│  Sessions:                              │
│  ✓ HttpOnly cookies                     │
│  ✓ Secure flag (HTTPS only)             │
│  ✓ SameSite: strict                     │
│  ✓ 256-bit random tokens                │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│         DATA SECURITY                   │
├─────────────────────────────────────────┤
│  Database:                              │
│  ⚠️ Currently: JSON file (insecure!)    │
│  ✓ File permissions: 600 (owner only)   │
│  ✗ No encryption at rest                │
│  ✗ No backup encryption                 │
│                                         │
│  API Keys:                              │
│  ✓ Stored in .env.local                 │
│  ⚠️ Shared across instances (risky)     │
│  ✗ Not rotated automatically            │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│      INFRASTRUCTURE SECURITY            │
├─────────────────────────────────────────┤
│  VPS (Hetzner):                         │
│  ✓ SSH key auth only                    │
│  ✓ Firewall rules                       │
│  ✓ Auto-updates enabled                 │
│  ✓ Isolated Docker networks             │
│                                         │
│  Docker:                                │
│  ✓ User namespaces                      │
│  ✓ Resource limits                      │
│  ✓ No privileged containers             │
│  ✓ Read-only root filesystem            │
└─────────────────────────────────────────┘
```

---

## Scaling Strategy

```
Current State (MVP)
┌────────────────────┐
│ 1 Server           │
│ • Main app         │
│ • JSON database    │
│ • PM2              │
└────────────────────┘
        │
        │ Handles: 1-50 users
        │ Cost: €20/month
        │
        ▼

Phase 1: Database Migration
┌────────────────────┐
│ 1 Server           │
│ • Main app         │
│ • PostgreSQL ◄─────┼─ Supabase/Neon
│ • PM2              │
└────────────────────┘
        │
        │ Handles: 50-500 users
        │ Cost: €20/month (free DB tier)
        │
        ▼

Phase 2: Load Balancing
┌────────────────────┐
│ Load Balancer      │
│ (Cloudflare)       │
└─────┬──────┬───────┘
      │      │
      ▼      ▼
┌────────┐ ┌────────┐
│ Server1│ │ Server2│
│ App    │ │ App    │
└────┬───┘ └───┬────┘
     │         │
     └────┬────┘
          ▼
┌──────────────────┐
│ PostgreSQL       │
│ (managed)        │
└──────────────────┘
        │
        │ Handles: 500-5K users
        │ Cost: €100-200/month
        │
        ▼

Phase 3: Microservices
┌───────────────────┐
│ API Gateway       │
└─────┬─┬─┬─┬───────┘
      │ │ │ │
  ┌───┘ │ │ └────┐
  │     │ │      │
  ▼     ▼ ▼      ▼
┌───┐ ┌──┐ ┌──┐ ┌────┐
│Web│ │API│ │Wkr│ │Prov│
└───┘ └──┘ └──┘ └────┘
  │     │    │     │
  └─────┴────┴─────┘
          ▼
┌──────────────────┐
│ PostgreSQL + Redis│
└──────────────────┘
        │
        │ Handles: 5K-50K users
        │ Cost: €500-1000/month
        │
        ▼

Phase 4: Multi-Region
┌──────────────────────────────┐
│     Global Load Balancer      │
└──────────┬──────────┬────────┘
           │          │
    ┌──────┴─┐    ┌──┴──────┐
    │  US-E  │    │   EU    │
    │        │    │         │
    │ API    │    │  API    │
    │ Worker │    │  Worker │
    │ DB     │    │  DB     │
    └────────┘    └─────────┘
        │
        │ Handles: 50K+ users
        │ Cost: €2000+/month
        │
        ▼
```

